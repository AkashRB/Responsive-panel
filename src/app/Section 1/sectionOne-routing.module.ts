import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';

@NgModule({
    imports: [
        RouterModule.forChild([
            { path: 'sectionOne', component:  }])
    ],
    exports: [RouterModule]
})
export class SectionOneRoutingModule { }
